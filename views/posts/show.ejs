<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="card text-center mb-3 col-md-4 mx-auto" style="width: 18rem">
      <div class="card-body">
        <h5 class="card-title"><%= post.heading%></h5>
        <p>created by=<i><%=post.created_by.username%></i></p>
        <p class="card-text"><%= post.text%></p>
        <br />
        <p>
          created at=
          <%=post.created_at.toString().split("").slice(0,25).join("")%>
        </p>
        <p>
          updated at=
          <%=post.updated_at.toString().split("").slice(0,25).join("")%>
        </p>
        <a href="/posts/<%=post._id%>/edit"
          ><button class="btn btn-primary">Edit</button></a
        >
        <br />
        <br />
        <form action="/posts/<%=post._id%>?_method=DELETE" method="POST">
          <button class="btn btn-primary">Delete</button>
        </form>

        <!-- <a href="#" class="btn btn-primary">Go somewhere</a> -->
      </div>
    </div>
  </body>
</html>
